# ギターコード表示アプリ 全体設計

## 1. 目的とスコープ

一覧から選ぶだけでギターコードのタブ譜（ダイアグラム）を表示する、シンプルなウェブアプリ。

- **やること**: コード選択 → タブ譜表示 → 音で確認 →（任意で）音声入力によるコード指定
- **やらないこと**: 曲の管理、コード進行の編集、楽譜（五線譜）表示、ログイン、サーバサイド処理

**設計方針**: バックエンドなしの完全クライアントサイド。静的ホスティング（GitHub Pages 等）に置くだけで動く。

---

## 2. 機能要件

### F-1 コード選択（3ステップ）

```
[Step 1] キー選択     A B C D E F G （+ ♯/♭ トグル）
              ↓
[Step 2] 種別選択     Major / Minor
              ↓
[Step 3] テンション   なし / 7 / M7 / 6 / sus4 / add9 / 9 / dim / aug ...
              ↓
        コード確定 → タブ譜表示
```

- 各ステップは常に表示しっぱなし（アコーディオンではなく 3 段の一覧）。前のステップを押し直すと即座に再確定する。
- Step 2 で Minor を選ぶとテンションの選択肢が m7 / mM7 / m6 / m7♭5 / dim7 に切り替わる。
- ステップ 3 は「なし」がデフォルト選択済み → Step 2 まで押せばコードが確定する。

### F-2 タブ譜（コードダイアグラム）表示

- 6弦 × 4〜5フレットのダイアグラム（SVG 描画）
- 押さえるポジション、開放弦（○）、ミュート弦（×）、バレー、指番号、開始フレット位置
- 同じコードに複数フォーム（開放ポジション / ローポジション / ハイポジション）がある場合は左右スワイプ・タブで切替
- テキスト表記（例: `x32010`）も併記

### F-3 音の確認

- 「再生」ボタンで表示中のフォームを鳴らす
- 再生モード: ストローク（下から順に少しずらす）/ アルペジオ / 全弦同時
- 弦を個別にタップしてもその音が鳴る

### F-4 音声入力（オプション機能）

```
（音声）「いーまいなー」 → 【コード】Em を表示
```

- マイクボタン押下 → 音声認識 → コード名にパース → 該当コードを確定表示
- 認識できなかった / 曖昧な場合は候補を出して選ばせる
- 非対応ブラウザではマイクボタンを非表示（機能自体は無くても成立する設計）

---

## 3. 技術選定

| 領域 | 採用 | 理由 |
|---|---|---|
| 言語 | TypeScript | コード定義のデータ構造を型で守りたい |
| UI | React 18 | 3ステップの選択状態と表示の同期が素直に書ける |
| ビルド | Vite | 設定ゼロ・静的出力・GitHub Pages にそのまま置ける |
| 状態管理 | `useReducer` 1本（Context 経由） | 状態は「選択中のコード」だけ。ライブラリ不要 |
| ダイアグラム描画 | 自前 SVG コンポーネント | 外部ライブラリ不要な規模。見た目を完全に制御できる |
| 音の再生 | Web Audio API（自前シンセ） | 音源ファイル不要 = 配信サイズが小さくオフラインでも鳴る |
| 音声認識 | Web Speech API (`SpeechRecognition`) | ブラウザ内蔵・無料・サーバ不要 |
| スタイル | CSS Modules（または素の CSS 変数） | 規模的にフレームワーク不要 |
| テスト | Vitest | 特に「音声→コード」パーサとコード生成ロジックに必須 |

> 代替案: ビルドレス（単一 HTML + ES Modules）でも実現可能だが、パーサとコード生成にテストを書きたいので Vite を推奨。

---

## 4. アーキテクチャ

```
┌─────────────────────────────────────────────────────┐
│                    UI Layer (React)                  │
│  KeySelector → QualitySelector → TensionSelector     │
│  ChordDiagram / FormTabs / PlayButton / MicButton    │
└───────────────┬─────────────────────────────────────┘
                │ ChordSelection { root, quality, tension }
┌───────────────▼─────────────────────────────────────┐
│                  Domain Layer (純関数)                │
│  chord/       コードID ⇄ 表示名 ⇄ 構成音              │
│  voicing/     コード → 押弦フォーム[] を生成           │
│  speech/      認識テキスト → ChordSelection にパース   │
└───────────────┬─────────────────────────────────────┘
┌───────────────▼─────────────────────────────────────┐
│              Platform Adapter (副作用)                │
│  audio/       Web Audio API ラッパ（発音・ストローク） │
│  recognition/ Web Speech API ラッパ（開始/停止/結果）  │
└─────────────────────────────────────────────────────┘
```

Domain Layer は DOM にもブラウザ API にも依存しない純関数群にして、Vitest で単体テストする。

### ディレクトリ構成

```
src/
  main.tsx
  App.tsx
  state/
    chordSelection.ts        # reducer + Context
  domain/
    chord/
      types.ts               # Root, Quality, Tension, ChordSelection
      catalog.ts             # 選択肢一覧（UI が並べる元データ）
      naming.ts              # 表示名生成 (E + minor + 7 → "Em7")
      notes.ts               # 構成音（度数→半音）
    voicing/
      shapes.ts              # 可動シェイプ・テンプレート
      openChords.ts          # 開放弦フォームの例外テーブル
      generate.ts            # ChordSelection → Voicing[]
      types.ts               # Voicing 型
    speech/
      normalize.ts           # 全角/カナ/記号の正規化
      dictionary.ts          # 読み → ルート/種別/テンション
      parse.ts               # テキスト → 候補[] （信頼度付き）
  platform/
    audio/
      engine.ts              # AudioContext の生成・管理
      pluck.ts               # Karplus-Strong による撥弦音
      strum.ts               # Voicing → 発音スケジュール
    recognition/
      useSpeechRecognition.ts
  components/
    selectors/  KeySelector.tsx / QualitySelector.tsx / TensionSelector.tsx
    diagram/    ChordDiagram.tsx / FretGrid.tsx / FingerDot.tsx / BarreBar.tsx
    controls/   PlayButton.tsx / MicButton.tsx / VoiceResultSheet.tsx
  styles/
```

---

## 5. データモデル

```ts
// 12音（表示は ♯/♭ 切替）
type Root = 'C'|'C#'|'D'|'D#'|'E'|'F'|'F#'|'G'|'G#'|'A'|'A#'|'B';

type Quality = 'major' | 'minor';

// Step3。quality によって選べるものが変わる
type Tension =
  | 'none' | '7' | 'M7' | '6' | '9' | 'add9' | 'sus4' | 'sus2' | 'aug'
  | 'm7b5' | 'dim7';   // minor 系でのみ提示

interface ChordSelection {
  root: Root;
  quality: Quality;
  tension: Tension;
}

interface Voicing {
  /** 6弦→1弦の順。null = ミュート(×), 0 = 開放(○), 1.. = フレット番号 */
  frets: (number | null)[];
  /** 同上。1=人差し指 … 4=小指, null=押さえない */
  fingers: (number | null)[];
  /** ダイアグラムの左端フレット（1 なら通常表示、5 なら "5fr" 表記） */
  baseFret: number;
  barres: { fret: number; fromString: number; toString: number }[];
  /** 'open' | 'barreE' | 'barreA' などフォームの由来 */
  label: string;
}
```

### コードフォームの持ち方

**全コードを手打ちしない。** 「可動シェイプ + 開放形の例外テーブル」で生成する。

1. **可動シェイプ（6弦ルート / 5弦ルート）**をルート相対のオフセットで定義し、ルート音の位置まで平行移動する。

   | 種別 | 6弦ルート (E形) | 5弦ルート (A形) |
   |---|---|---|
   | major | `0 2 2 1 0 0` | `x 0 2 2 2 0` |
   | minor | `0 2 2 0 0 0` | `x 0 2 2 1 0` |
   | 7 | `0 2 0 1 0 0` | `x 0 2 0 2 0` |
   | M7 | `0 2 1 1 0 0` | `x 0 2 1 2 0` |
   | m7 | `0 2 0 0 0 0` | `x 0 2 0 1 0` |
   | 6 | `0 2 2 1 2 0` | `x 0 2 2 2 2` |
   | sus4 | `0 2 2 2 0 0` | `x 0 2 2 3 0` |
   | … | | |

   数字はルートフレットからの相対値。例: `G major` = 6弦ルートが 3fr なので `3 5 5 4 3 3`。

2. **開放形テーブル**で、よく使う C / A / G / E / D / Em / Am / Dm 等の押さえやすい形を上書き・追加する。

3. 生成結果を「開放形 → 6弦ルート → 5弦ルート」の順に並べ、フォーム切替タブに出す。

この方式なら定義データは数十行で済み、`root × quality × tension` の全組み合わせを網羅できる。

---

## 6. UI / 画面設計

**片手にギターを持った状態で操作する**前提 → モバイルファースト、タップ領域は 44px 以上、1画面完結（スクロールなし）を目指す。

```
┌──────────────────────────────┐
│  Guitar Chords          [🎤] │  ← ヘッダ + マイク
├──────────────────────────────┤
│  KEY   [A][B][C][D][E][F][G] │  ← Step1（♯/♭トグル付き）
│  TYPE  [ Major ][ Minor ]    │  ← Step2
│  ADD   [-][7][M7][6][sus4]…  │  ← Step3（横スクロール）
├──────────────────────────────┤
│            E m                │  ← 確定コード名（大きく）
│      ○ ○   ○ ○ ○              │
│      ┌─┬─┬─┬─┐               │  ← SVG ダイアグラム
│      │ │2│3│ │               │
│      ├─┼─┼─┼─┤               │
│      022000                   │  ← テキスト表記
│   ◀ 開放形 | 7fr | 12fr ▶     │  ← フォーム切替
├──────────────────────────────┤
│   [ ▶ 再生 ]  ストローク ▾     │
└──────────────────────────────┘
```

状態遷移は単一の reducer で管理:

```ts
type Action =
  | { type: 'SELECT_ROOT'; root: Root }
  | { type: 'SELECT_QUALITY'; quality: Quality }
  | { type: 'SELECT_TENSION'; tension: Tension }
  | { type: 'SELECT_VOICING_INDEX'; index: number }
  | { type: 'APPLY_VOICE_RESULT'; selection: ChordSelection };
```

- `SELECT_QUALITY` で、現在のテンションが新しい種別に存在しなければ `none` にフォールバック。
- `SELECT_*` のたびに `voicingIndex` を 0 に戻す。

---

## 7. 音声入力の設計

Web Speech API の結果は「いーまいなー」「Eマイナー」「イー マイナー」「1マイナー」など**揺れが激しい**。そのため単純な文字列一致ではなく、正規化 → 辞書マッチ → スコアリングの 3 段で処理する。

```
音声認識 raw text
   ↓ normalize()      全角→半角 / カタカナ→ひらがな / 空白・記号除去 / 小文字化
"いーまいなー"
   ↓ tokenize + match ルート辞書・変化記号辞書・種別辞書・テンション辞書を前方から貪欲マッチ
{ root: 'E', quality: 'minor', tension: 'none', score: 0.9 }
   ↓ 確定 or 候補提示
【コード】Em
```

### 読み辞書（抜粋）

| 対象 | 受け付ける読み |
|---|---|
| A | `えー` `えい` `え` `a` |
| B | `びー` `び` `b` |
| C | `しー` `すぃー` `c` |
| D | `でぃー` `で` `d` |
| E | `いー` `い` `e` `1`（誤変換対策） |
| F | `えふ` `f` |
| G | `じー` `じ` `g` |
| ♯ | `しゃーぷ` `#` `♯` |
| ♭ | `ふらっと` `♭` `b`（ルート直後のみ） |
| minor | `まいなー` `まいなあ` `たんちょう` `m` |
| major | `めじゃー` `ちょうちょう`（省略時のデフォルト） |
| 7 | `せぶんす` `せぶん` `なな` `7` |
| M7 | `めじゃーせぶんす` `まる7` `M7` |
| sus4 | `さすふぉー` `さす4` |
| dim | `でぃむ` `でぃみにっしゅ` |
| aug | `おーぐ` `おーぐめんと` |

### 曖昧性の扱い

- `b` は「B」か「♭」か曖昧 → **位置で判定**（先頭なら B、ルートの直後なら ♭）
- ルートが特定できなければ**確定しない**。候補が複数なら下からシートを出して選択させる
- 認識結果は常に画面に「聞き取り: いーまいなー」と表示し、誤認識をユーザーが把握できるようにする

### API まわり

- `SpeechRecognition.lang = 'ja-JP'`, `continuous = false`, `interimResults = true`（途中経過を表示）
- HTTPS 必須。`navigator.mediaDevices` の権限拒否 / `webkitSpeechRecognition` 不在は**機能無効化してフォールバック**（一覧選択は常に使える）
- Chrome / Edge が主対象。Safari・Firefox は非対応の可能性があるため機能検出必須

---

## 8. 音再生の設計

### ピッチ計算

レギュラーチューニングの開放弦 MIDI ノート番号（6弦→1弦）:

```
E2=40, A2=45, D3=50, G3=55, B3=59, E4=64
midi  = openString[i] + fret
freq  = 440 * 2 ** ((midi - 69) / 12)
```

### 音源

サンプル音源は使わず、**Karplus-Strong 法**（ノイズバースト + 遅延フィードバックのローパス）で撥弦音を合成する。

- 配信サイズ ≒ 0、オフライン動作、任意のピッチに対応
- 実装は `AudioWorklet`（不可なら `ScriptProcessor` / `BufferSource` へフォールバック）
- 簡易版として「複数倍音の `OscillatorNode` + ADSR エンベロープ」でも可。まずはこちらで作って差し替え可能にする

### 再生モード

| モード | 実装 |
|---|---|
| ストローク（ダウン） | 6弦→1弦の順に 25〜40ms ずつ遅らせて発音 |
| アルペジオ | 各弦を 250〜350ms 間隔で発音 |
| 同時 | 遅延なし |

- ミュート弦（`null`）は発音しない
- iOS 対策: 最初のユーザー操作で `AudioContext.resume()` を必ず呼ぶ
- 再生中に別コードを選んだら前の音をフェードアウトさせて停止

---

## 9. テスト方針

Domain Layer が純関数なので、ここに厚くテストを置く。

- `naming`: `{E, minor, 7}` → `"Em7"`、`{A#, major, M7}` → `"A♯M7"`
- `generate`: 全 `root × quality × tension` で 1 つ以上のフォームが生成されること／フレット番号が 0〜15 に収まること／構成音が理論値と一致すること
- `speech/parse`: 「いーまいなー」→ Em、「しーせぶんす」→ C7、「えふしゃーぷまいなーせぶん」→ F♯m7、聞き取り失敗ケースで候補が返ること
- UI は主要フロー（3ステップ選択 → ダイアグラム表示）だけ軽くスモークテスト

---

## 10. 実装フェーズ

| Phase | 内容 | 成果 |
|---|---|---|
| **0** | Vite + React + TS 雛形、Lint/テスト設定、GitHub Pages デプロイ | 空ページが公開される |
| **1** | 型定義 / カタログ / シェイプ生成 / SVG ダイアグラム / 3ステップ UI | **選んで見られる MVP** |
| **2** | Web Audio 再生（同時→ストローク→アルペジオ） | 音で確認できる |
| **3** | 音声入力（正規化・辞書・パーサ・候補 UI） | 「いーまいなー」→ Em |
| **4** | 複数フォーム切替、テキスト表記、レスポンシブ調整 | 実用レベル |
| **5**（任意） | PWA 化（オフライン）、お気に入り、左利き反転、カポ対応、ダーク/ライト | — |

Phase 1 だけで「表示するだけのアプリ」として完成する構成にし、2 以降は独立して足せるようにする。

---

## 11. 主な設計判断とリスク

| 判断 | 理由 |
|---|---|
| バックエンドなし | 全処理がクライアントで完結。運用コストゼロ |
| コードフォームを生成方式に | 数百件の手打ちデータを持たずに全組み合わせを網羅できる。ただし「押さえやすさ」は開放形テーブルで補う |
| 音源ファイルを持たない | 初回ロードが速く、オフラインでも鳴る |
| 音声入力を完全にオプション扱い | Web Speech API はブラウザ依存が強い。無くてもアプリが成立する構造にする |

**リスクと対策**

- 音声認識のブラウザ非対応 → 機能検出して非表示。一覧選択は常時利用可
- 日本語の認識揺れ → 辞書を外出しして追加しやすくする + 候補提示で救う
- 生成フォームが押さえにくい → 開放形テーブルで上書き、フォーム一覧を「押さえやすい順」に並べる
- iOS の音声再生制限 → ユーザー操作起点で `AudioContext` を初期化
