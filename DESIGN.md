# ギターコード表示アプリ 全体設計

## 1. 目的とスコープ

一覧から選ぶだけでギターコードのタブ譜（ダイアグラム）を表示する、シンプルなウェブアプリ。

- **やること**: コード選択 → タブ譜表示 → 音で確認 →（任意で）音声入力によるコード指定
- **やらないこと**: 曲の管理、コード進行の編集、楽譜（五線譜）表示、ログイン、サーバサイド処理

**設計方針**: バックエンドなしの完全クライアントサイド。静的ホスティング（Cloudflare Pages）に置くだけで動く。

---

## 2. 機能要件

### F-1 コード選択（3ステップ）

```
[Step 1] キー選択     A B C D E F G （下段に C♯ D♯ F♯ G♯ A♯ / ♯♭ 表記トグル）
              ↓
[Step 2] 種別選択     Major / Minor
              ↓
[Step 3] テンション   なし / 7 / M7 / 6 / sus4    （Minor 時は なし / 7 / m7♭5）
              ↓
        コード確定 → タブ譜表示
```

- 各ステップは常に表示しっぱなし（アコーディオンではなく 3 段の一覧）。前のステップを押し直すと即座に再確定する。
- Step 2 で Minor を選ぶとテンションの選択肢が切り替わり、選べなくなったテンションは「なし」に落とす。
- Step 3 は「なし」がデフォルト選択済み → Step 2 まで押せばコードが確定する。
- ♯/♭ トグルは**表示表記だけ**を切り替える（鳴るコードは変わらない）。

### F-2 タブ譜（コードダイアグラム）表示

- 6弦 × 4〜5フレットのダイアグラム（SVG 描画）
- 押さえるポジション、開放弦（○）、ミュート弦（×）、バレー、指番号、開始フレット位置
- 同じコードに複数フォーム（開放ポジション / ローポジション / ハイポジション）がある場合は左右スワイプ・タブで切替
- テキスト表記（例: `x32010`）も併記

### F-3 音の確認

- 「再生」ボタンで表示中のフォームを鳴らす
- 再生モード: ストローク（下から順に少しずらす）/ アルペジオ
- 弦を個別にタップしてもその音が鳴る

### F-4 音声入力（オプション機能）

```
（音声）「いーまいなー」 → 【コード】Em を表示
```

- マイクボタン押下 → 音声認識 → コード名にパース → 該当コードを確定表示
- 認識できなかった / 曖昧な場合は候補を出して選ばせる
- 非対応ブラウザではマイクボタンを非表示（機能自体は無くても成立する設計）

---

## 3. 技術選定

| 領域 | 採用 | 理由 |
|---|---|---|
| 言語 | TypeScript | コード定義のデータ構造を型で守りたい |
| UI | React 18 | 3ステップの選択状態と表示の同期が素直に書ける |
| ビルド | Vite | 設定ゼロ・静的出力・Cloudflare Pages にそのまま置ける |
| 状態管理 | `useReducer` 1本（Context 経由） | 状態は「選択中のコード」だけ。ライブラリ不要 |
| ダイアグラム描画 | 自前 SVG コンポーネント | 外部ライブラリ不要な規模。見た目を完全に制御できる |
| 音の再生 | Web Audio API（自前シンセ） | 音源ファイル不要 = 配信サイズが小さくオフラインでも鳴る |
| 音声認識 | Web Speech API (`SpeechRecognition`) | ブラウザ内蔵・無料・サーバ不要 |
| スタイル | CSS Modules（または素の CSS 変数） | 規模的にフレームワーク不要 |
| テスト | Vitest | 特に「音声→コード」パーサとコード生成ロジックに必須 |
| ホスティング | Cloudflare Pages | 静的出力をそのまま配信。常時 HTTPS なので Web Speech API / Web Audio の要件も満たす |

> 代替案: ビルドレス（単一 HTML + ES Modules）でも実現可能だが、パーサとコード生成にテストを書きたいので Vite を推奨。

---

## 4. アーキテクチャ

```
┌─────────────────────────────────────────────────────┐
│                    UI Layer (React)                  │
│  KeySelector → QualitySelector → TensionSelector     │
│  ChordDiagram / FormTabs / PlayButton / MicButton    │
└───────────────┬─────────────────────────────────────┘
                │ ChordSelection { root, quality, tension }
┌───────────────▼─────────────────────────────────────┐
│                  Domain Layer (純関数)                │
│  chord/       コードID ⇄ 表示名 ⇄ 構成音              │
│  voicing/     コード → 押弦フォーム[] を生成           │
│  speech/      認識テキスト → ChordSelection にパース   │
└───────────────┬─────────────────────────────────────┘
┌───────────────▼─────────────────────────────────────┐
│              Platform Adapter (副作用)                │
│  audio/       Web Audio API ラッパ（発音・ストローク） │
│  recognition/ Web Speech API ラッパ（開始/停止/結果）  │
└─────────────────────────────────────────────────────┘
```

Domain Layer は DOM にもブラウザ API にも依存しない純関数群にして、Vitest で単体テストする。

### ディレクトリ構成

```
src/
  main.tsx
  App.tsx
  state/
    chordSelection.ts        # reducer + Context
  domain/
    chord/
      types.ts               # Root, Quality, Tension, ChordSelection
      catalog.ts             # 選択肢一覧（UI が並べる元データ）
      naming.ts              # 表示名生成 (E + minor + 7 → "Em7")
      notes.ts               # 構成音（度数→半音）
    voicing/
      shapes.ts              # 可動シェイプ・テンプレート
      openChords.ts          # 開放弦フォームの例外テーブル
      generate.ts            # ChordSelection → Voicing[]
      types.ts               # Voicing 型
    speech/
      normalize.ts           # 全角/カナ/記号の正規化
      dictionary.ts          # 読み → ルート/種別/テンション
      parse.ts               # テキスト → 候補[] （信頼度付き）
  platform/
    audio/
      engine.ts              # AudioContext の生成・管理
      pluck.ts               # Karplus-Strong による撥弦音
      strum.ts               # Voicing → 発音スケジュール
    recognition/
      useSpeechRecognition.ts
  components/
    selectors/  KeySelector.tsx / QualitySelector.tsx / TensionSelector.tsx
    diagram/    ChordDiagram.tsx / FretGrid.tsx / FingerDot.tsx / BarreBar.tsx
    controls/   PlayButton.tsx / MicButton.tsx / VoiceResultSheet.tsx
  styles/
```

---

## 5. データモデル

```ts
// 12音（表示は ♯/♭ 切替）
type Root = 'C'|'C#'|'D'|'D#'|'E'|'F'|'F#'|'G'|'G#'|'A'|'A#'|'B';

type Quality = 'major' | 'minor';

// Step3。quality によって選べるものが変わる
type Tension =
  | 'none' | '7' | 'M7' | '6' | '9' | 'add9' | 'sus4' | 'sus2' | 'aug'
  | 'm7b5' | 'dim7';   // minor 系でのみ提示

interface ChordSelection {
  root: Root;
  quality: Quality;
  tension: Tension;
}

interface Voicing {
  /** 6弦→1弦の順。null = ミュート(×), 0 = 開放(○), 1.. = フレット番号 */
  frets: (number | null)[];
  /** 同上。1=人差し指 … 4=小指, null=押さえない */
  fingers: (number | null)[];
  /** ダイアグラムの左端フレット（1 なら通常表示、5 なら "5fr" 表記） */
  baseFret: number;
  barres: { fret: number; fromString: number; toString: number }[];
  /** 'open' | 'barreE' | 'barreA' などフォームの由来 */
  label: string;
}
```

### コードフォームの持ち方

**全コードを手打ちしない。** 「可動シェイプ + 開放形の例外テーブル」で生成する。

1. **可動シェイプ（6弦ルート / 5弦ルート）**をルート相対のオフセットで定義し、ルート音の位置まで平行移動する。

   | 種別 | 6弦ルート (E形) | 5弦ルート (A形) |
   |---|---|---|
   | major | `0 2 2 1 0 0` | `x 0 2 2 2 0` |
   | minor | `0 2 2 0 0 0` | `x 0 2 2 1 0` |
   | 7 | `0 2 0 1 0 0` | `x 0 2 0 2 0` |
   | M7 | `0 2 1 1 0 0` | `x 0 2 1 2 0` |
   | m7 | `0 2 0 0 0 0` | `x 0 2 0 1 0` |
   | 6 | `0 2 2 1 2 0` | `x 0 2 2 2 2` |
   | sus4 | `0 2 2 2 0 0` | `x 0 2 2 3 0` |
   | … | | |

   数字はルートフレットからの相対値。例: `G major` = 6弦ルートが 3fr なので `3 5 5 4 3 3`。

2. **開放形テーブル**で、よく使う C / A / G / E / D / Em / Am / Dm 等の押さえやすい形を上書き・追加する。

3. 生成結果の重複を除き、**押さえやすさで並べ替えて**フォーム切替タブに出す。

   シェイプの種類順（開放形 → 6弦ルート → 5弦ルート）で並べると、
   同じコードでも 12fr のバレーが 7fr より先に来てしまう。負担そのものを評価する:

   ```
   難易度 = 最低フレット          （ローポジションほど楽）
          + 指の広がり            （最高フレット − 最低フレット）
          + バレーがあれば 4
          + ミュート弦数 × 0.5
          − 開放弦数              （開放弦が多いほど楽）
   ```

この方式なら定義データは数十行で済み、`root × quality × tension` の全組み合わせを網羅できる。

---

## 6. UI / 画面設計

**片手にギターを持った状態で操作する**前提 → モバイルファースト、タップ領域は 44px 以上、1画面完結（スクロールなし）を目指す。

スマホでは 3 ステップぶんの一覧が縦を占有してコード表示が画面外に出てしまうため、
**コード指定エリアを開閉式にして、閉じているときはコード表示に画面を明け渡す**。

```
【展開時】                          【折りたたみ時】
┌──────────────────────────────┐   ┌──────────────────────────────┐
│  Guitar Chords          [🎤] │   │  Guitar Chords          [🎤] │
├──────────────────────────────┤   ├──────────────────────────────┤
│  コードを選ぶ         Em7  ⌃ │   │  コードを選ぶ         Em7  ⌄ │ ← 常時表示
├──────────────────────────────┤   ├──────────────────────────────┤
│  1 キー  [A][B][C][D][E][F][G]│   │             Em7               │ ← 常に中央
│          [C♯][D♯][F♯][G♯][A♯]│   │          [minor]  [★]         │
│  2 種別  [ Major ][ Minor ]  │   │        ○   ○ ○ ○ ○            │
│  3 テンション [-][7][m7♭5]   │   │   ‹    ┌─┬─┬─┬─┐     ›        │
│  カポ    [-][1][2][3]...     │   │        │2│ │ │ │             │ ← 大きく表示
├──────────────────────────────┤   │        └─┴─┴─┴─┘             │
│            Em7                │   │         020000                │
│         （画面外）             │   │   [開放][7fr][12fr]           │
└──────────────────────────────┘   │   [ ▶ 再生 ][ストローク|アルペジオ]│
                                   └──────────────────────────────┘
```

- 折りたたみバーには**現在のコード名**を出し、閉じていても何を選んでいるかが分かるようにする
- 折りたたみ時はコード名・ダイアグラム・タブ表記を一段大きくして、譜面としての可読性を上げる
- 弦は低音弦ほど太く描く（実際のギターに合わせ、6弦→1弦で 1.9px → 0.85px）
- ダイアグラムの左右に半音送りのボタンを置き、**同じ操作を横スワイプでも行える**ようにする
- 720px 以上（2カラム表示）では開閉する理由がないので、**トグル自体を隠して常時展開**する
- **高さ 540px 以下**（スマホ横向きなど）はコード表示を詰め、再生ボタンとモード切替を横並びにする。
  横向きのスマホは幅が広いので 2 カラムにはなるが、高さが足りずコード表示がスクロールに追い出されるため
- Step3（テンション）まで選べばコードが確定するので、**そのタップで自動的に折りたたむ**
- 開閉状態は UI ローカルの状態（`useState`）。コード選択の reducer とは混ぜない

状態遷移は単一の reducer で管理:

```ts
type Action =
  | { type: 'SELECT_ROOT'; root: Root }
  | { type: 'SELECT_QUALITY'; quality: Quality }
  | { type: 'SELECT_TENSION'; tension: Tension }
  | { type: 'SELECT_VOICING_INDEX'; index: number }
  | { type: 'APPLY_VOICE_RESULT'; selection: ChordSelection };
```

- `SELECT_QUALITY` で、現在のテンションが新しい種別に存在しなければ `none` にフォールバック。
- `SELECT_*` のたびに `voicingIndex` を 0 に戻す。
- 開閉状態は「どのコードを表示しているか」に影響しないため reducer には持たせない。

---

## 7. 音声入力の設計

Web Speech API の結果は「いーまいなー」「Eマイナー」「イー マイナー」「1マイナー」など**揺れが激しい**。そのため単純な文字列一致ではなく、正規化 → 辞書マッチ → スコアリングの 3 段で処理する。

```
音声認識 raw text
   ↓ normalize()      全角→半角 / カタカナ→ひらがな / 空白・記号除去 / 小文字化
"いーまいなー"
   ↓ tokenize + match ルート辞書・変化記号辞書・種別辞書・テンション辞書を前方から貪欲マッチ
{ root: 'E', quality: 'minor', tension: 'none', score: 0.9 }
   ↓ 確定 or 候補提示
【コード】Em
```

### 読み辞書（抜粋）

| 対象 | 受け付ける読み |
|---|---|
| A | `えー` `えい` `え` `a` |
| B | `びー` `び` `b` |
| C | `しー` `すぃー` `c` |
| D | `でぃー` `で` `d` |
| E | `いー` `い` `e` `1`（誤変換対策） |
| F | `えふ` `f` |
| G | `じー` `じ` `g` |
| ♯ | `しゃーぷ` `#` `♯` |
| ♭ | `ふらっと` `♭` `b`（ルート直後のみ） |
| minor | `まいなー` `まいなあ` `たんちょう` `m` |
| major | `めじゃー` `ちょうちょう`（省略時のデフォルト） |
| 7 | `せぶんす` `せぶん` `なな` `7` |
| M7 | `めじゃーせぶんす` `まる7` `M7` |
| sus4 | `さすふぉー` `さす4` |
| dim | `でぃむ` `でぃみにっしゅ` |
| aug | `おーぐ` `おーぐめんと` |

### 曖昧性の扱い

- `b` は「B」か「♭」か曖昧 → **位置で判定**（先頭なら B、ルートの直後なら ♭）
- ルートが特定できなければ**確定しない**（`parseChord` が null を返す）
- 認識結果は常に画面に「聞き取り: いーまいなー」と表示し、誤認識をユーザーが把握できるようにする

**候補は音声認識自身に出させる。** `maxAlternatives = 5` で認識候補を複数受け取り、
それぞれをパースして同じコードに畳む。これで「聞き間違い」を人工的に作らずに候補が得られる。

**確信度**は「聞き取った文字列のうち、コードとして解釈できた割合」で算出する。

| 状況 | 扱い |
|---|---|
| 候補が 1 つ かつ 確信度 ≥ 0.8 | 確認を挟まずそのまま適用する |
| 候補が複数 | 下からシートを出して選ばせる |
| 候補ゼロ（ルートが取れない） | 「コードとして聞き取れませんでした」＋言い方の例を出す |

小文字化は**してはいけない**。`M7`（メジャーセブンス）と `m7`（マイナーセブンス）の区別が失われる。

### API まわり

- `SpeechRecognition.lang = 'ja-JP'`, `continuous = false`, `interimResults = true`（途中経過を表示）,
  `maxAlternatives = 5`（候補の取得元）
- 確定結果を受け取ったら **`onend` を待たずに自分で `stop()` する**。
  待つと結果を反映したあとも「聞き取り中」の表示が残る
- HTTPS 必須。`navigator.mediaDevices` の権限拒否 / `webkitSpeechRecognition` 不在は**機能無効化してフォールバック**（一覧選択は常に使える）
- Chrome / Edge が主対象。Safari・Firefox は非対応の可能性があるため機能検出必須

---

## 8. 音再生の設計

### ピッチ計算

レギュラーチューニングの開放弦 MIDI ノート番号（6弦→1弦）:

```
E2=40, A2=45, D3=50, G3=55, B3=59, E4=64
midi  = openString[i] + fret
freq  = 440 * 2 ** ((midi - 69) / 12)
```

### 音源

サンプル音源は使わず、**Karplus-Strong 法**（ノイズバースト + 遅延フィードバックのローパス）で撥弦音を合成する。

- 配信サイズ ≒ 0、オフライン動作、任意のピッチに対応
- 実装は **`AudioBuffer` に一括レンダリングして `AudioBufferSourceNode` で再生**する方式。
  `AudioWorklet` のようなリアルタイム処理より単純で、対応ブラウザの制約もない
- 1 音あたり 2.6 秒 × 48kHz ≒ 12 万サンプルの単純ループなので、6 弦ぶん生成しても 1〜2ms 程度。
  キャッシュを持たない代わりにメモリを一切占有しない
- 末尾は必ず振幅 0 まで落としてクリックノイズを防ぐ
- 各音は専用の `GainNode` を通し、停止時は 60ms でフェードアウトさせる（ブツ切りを防ぐ）

### 再生モード

| モード | 実装 |
|---|---|
| ストローク（ダウン） | 6弦→1弦の順に 25〜40ms ずつ遅らせて発音 |
| アルペジオ | 各弦を 250〜350ms 間隔で発音 |

- ミュート弦（`null`）は発音しない
- iOS 対策: 最初のユーザー操作で `AudioContext.resume()` を必ず呼ぶ
- 再生中に別コードを選んだら前の音をフェードアウトさせて停止

---

## 9. テスト方針

Domain Layer が純関数なので、ここに厚くテストを置く。

- `pitch`: MIDI 69 → 440Hz、6弦開放 → 82.41Hz、半音 = 2^(1/12) 倍
- `pluck`: 生成波形が有限・振幅内に収まる／**自己相関で狙ったピッチの周期になっている**／減衰する／末尾が 0
- `naming`: `{E, minor, 7}` → `"Em7"`、`{A#, major, M7}` → `"A♯M7"`
- `generate`: 全 `root × quality × tension` で 1 つ以上のフォームが生成されること／フレット番号が 0〜15 に収まること／構成音が理論値と一致すること／**押さえやすい順に並んでいること**
- `speech/parse`: 「いーまいなー」→ Em、「しーせぶんす」→ C7、「えふしゃーぷまいなーせぶん」→ F♯m7、
  `b` の曖昧性（`b` → B / `Bb` → B♭）、`M7` と `m7` の区別、ルート無しは null、無関係な発話は確信度が下がること
- UI は主要フロー（3ステップ選択 → ダイアグラム表示）だけ軽くスモークテスト

---

## 10. 実装フェーズ

| Phase | 内容 | 成果 |
|---|---|---|
| **0** ✅ | Vite + React + TS 雛形、テスト設定、Cloudflare Pages デプロイ設定 | ビルドして公開できる状態 |
| **1** ✅ | 型定義 / カタログ / シェイプ生成 / SVG ダイアグラム / 3ステップ UI | **選んで見られる MVP** |
| **2** ✅ | Web Audio 再生（ストローク / アルペジオ、弦の個別発音） | 音で確認できる |
| **3** ✅ | 音声入力（正規化・辞書・パーサ・候補 UI） | 「いーまいなー」→ Em |
| **4** ✅ | フォームの並び順（押さえやすさ順）、ダイアグラムの作り込み、レスポンシブ調整 | 実用レベル |
| **5** | PWA 化（オフライン）✅、カポ対応 ✅、お気に入り ✅、左利き反転、ダーク/ライト | — |

Phase 1 だけで「表示するだけのアプリ」として完成する構成にし、2 以降は独立して足せるようにする。

---

## 11. 主な設計判断とリスク

| 判断 | 理由 |
|---|---|
| バックエンドなし | 全処理がクライアントで完結。運用コストゼロ |
| コードフォームを生成方式に | 数百件の手打ちデータを持たずに全組み合わせを網羅できる。ただし「押さえやすさ」は開放形テーブルで補う |
| 音源ファイルを持たない | 初回ロードが速く、オフラインでも鳴る |
| Cloudflare Pages に静的配信 | ビルド成果物を置くだけ。`public/_headers` でキャッシュ制御まで完結する |
| 音声入力を完全にオプション扱い | Web Speech API はブラウザ依存が強い。無くてもアプリが成立する構造にする |

**リスクと対策**

- 音声認識のブラウザ非対応 → 機能検出して非表示。一覧選択は常時利用可
- 日本語の認識揺れ → 辞書を外出しして追加しやすくする + 候補提示で救う
- 生成フォームが押さえにくい → 開放形テーブルで上書き、フォーム一覧を「押さえやすい順」に並べる
- iOS の音声再生制限 → ユーザー操作起点で `AudioContext` を初期化

---

## 12. デプロイ（Cloudflare Pages）

完全な静的サイトなので Pages の標準設定でそのまま動く。

| 項目 | 値 |
|---|---|
| Framework preset | None（プリセットは使わず下の 2 つを直接入力する） |
| Build command | `npm run build` |
| Build output directory | `dist` |
| Node.js version | `.node-version`（22） |

- `vite.config.ts` の `base` は `/`（Pages はサイトルート配信のため、GitHub Pages のようなサブパス調整が不要）
- `public/_headers` がそのまま Pages のヘッダ設定になる（ハッシュ付きアセットの長期キャッシュ、`X-Content-Type-Options` など）
- Pages は常時 HTTPS。Phase 3 の Web Speech API はセキュアコンテキスト必須なので、この点でも都合がよい
- Git 連携にすれば push ごとにプレビューデプロイが立つため、フォームの見た目確認をブランチ単位で回せる

---

## 13. カポ対応

**ユーザーが選ぶのは常に「鳴らしたい実音」**。カポは開放弦を n 半音持ち上げるので、
押さえる形はその ぶんだけ低いコードになる。

```
選択（実音）  E♭          カポ 3fr
                ↓ shapeSelectionFor(selection, 3)
押さえる形     C           ← ダイアグラムはこちらを描く
                ↓ voicingMidiNotes(voicing, 3)
実際に鳴る音   E♭ G B♭     ← 再生はこちら
```

- ダイアグラムのフレット番号は**カポ基準**（カポがナットの位置に来る）。市販のコードブックと同じ流儀
- ナットはカポがあるときアクセント色のバーで描く
- コード名・構成音の表示は**実音**のまま。「押さえる形は C」を併記する
- 再生は実音。開放弦もカポで押さえられた高さで鳴る（`voicingMidiNotes` に capo を渡す）
- カポ位置は「どのコードを表示しているか」に影響するため reducer が持つ（0〜7fr）

## 14. PWA（オフライン対応）

配信物は `index.html` とハッシュ付きアセットだけで、取得すべきデータが何もない。
そのため丸ごとキャッシュすれば完全にオフラインで動く（合計 200KB 弱）。

- `public/manifest.webmanifest` — ホーム画面に追加したときの名前・アイコン・全画面表示
- `public/sw.js` — 依存ライブラリなしの手書き Service Worker
- アイコンは `public/icon.svg` から PNG（180 / 192 / 512）を生成して同梱

**キャッシュ戦略は 2 つだけ**。Service Worker で最も事故りやすいのは
「デプロイしたのに古い画面が出続ける」状態なので、そこを潰す形で決めている。

| 対象 | 戦略 | 理由 |
|---|---|---|
| 画面本体（ナビゲーション） | ネットワーク優先 | 更新が確実に届く。取得成功時にオフライン用フォールバックも更新する |
| ハッシュ付きアセット | キャッシュ優先 | 中身が変われば必ずファイル名も変わるので、陳腐化しない |

- `sw.js` 自身は `_headers` で `Cache-Control: no-cache`。ここをキャッシュされると更新が届かなくなる
- 登録は本番ビルドのみ（`import.meta.env.PROD`）。開発中にキャッシュで混乱しないようにする
- 登録に失敗してもアプリは通常どおり動く（オフライン対応が付かないだけ）

## 15. お気に入り

練習中に行き来するコードをすぐ呼び出せるようにする。サーバーは使わず `localStorage` に置く。

- 保存するのは**コードだけ**（root / quality / tension）。カポは別軸の設定なので含めない。
  カポ 3fr で E♭ を登録して、後でカポを外しても E♭ のまま表示される
- 上限 24 件。新しいものが先頭
- お気に入りバーは**1 件以上あるときだけ**表示する。
  折りたたみ時に 1 画面へ収める設計なので、使っていない人の縦を奪わない

**保存内容は信用しない。** 利用者が書き換えられるうえ、アプリ側の型が変わることもある。

| 状況 | 扱い |
|---|---|
| JSON として壊れている | お気に入り無しとして続行 |
| 配列でない | 同上 |
| 知らないコード値が混ざっている | **その要素だけ**捨てる |
| 同じコードが重複している | 1 つに畳む |
| `localStorage` 自体が使えない（プライベートモード・容量超過） | 読み書きとも握りつぶし、この画面のぶんは通常どおり動かす |

状態は `useFavorites`（`useState` + 保存領域への書き込み）に持たせ、コード選択の reducer とは分ける。
「どのコードを表示しているか」には影響しないため。

## 16. コード表示からの直接操作

指定エリアを開かずにコードを変えられるようにする。練習中は「隣のキー」「マイナーにする」の
2 つが圧倒的に多く、そのたびに 3 ステップの一覧を開き直すのは煩わしい。

```
              A♯m7♭5                 ← 常に中央
            [minor]  [★]             ← 名前の左右に置くと中央からずれるため下の行へ
   ‹      （ダイアグラム）      ›      ← 横スワイプでも同じ
```

- **半音送り**: 左スワイプで次（高い方）、右スワイプで前。左右のボタンでも同じ操作ができる
- **マイナー切替**: コード名の横のトグル。Step2 の選択と同じ状態を見ている（どちらを押しても同期する）
- **テンションは引き継ぐ**。`C7` のままキーを送れば `C♯7`、マイナーにすれば `Cm7`

移調は `transposeSelection(selection, semitones)` に集約する。
カポの読み替えも「マイナス方向の移調」なので同じ関数を使う。

### スワイプ実装上の注意

同じ領域に「弦をタップして鳴らす」操作が同居しているため、区別が要る。

| 課題 | 対処 |
|---|---|
| スワイプ終わりの click で弦が鳴ってしまう | スワイプ判定時にフラグを立て、直後の click を無視する |
| 縦スクロールを奪ってしまう | 横移動が縦移動より大きいときだけスワイプとみなす／`touch-action: pan-y` |
| 指が領域の外に出ると pointerup を取り逃がす | `setPointerCapture` でポインタを掴む |
| **ブラウザ標準のドラッグが始まると `pointercancel` が飛び、`pointerup` が来ない** | `dragstart` を `preventDefault` し、`user-select` / `-webkit-user-drag` も無効化する |

最後の 1 つは実機で踏んだ。SVG の上を左から右へドラッグすると Chromium がネイティブのドラッグを
開始してしまい、**片方向のスワイプだけが無反応**になる。閾値やしきい判定を疑いやすいが、
原因は pointer イベント列そのものが打ち切られていることにある。
